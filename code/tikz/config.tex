% -----------------------------------------------------------------------------
% imports
\newcommand{\rootpath}{../..}
\input{style}
\input{colors}
\usepackage{tikz}
\usetikzlibrary{
  decorations.pathreplacing,
  arrows.meta,
}
% -----------------------------------------------------------------------------
% tikz config
\tikzset{
  > = latex,
  df/.style = {
    draw = #1,
    fill = #1!20,
    inner sep = 0pt,
  },
  var/.style = {
    df = #1,
    inner sep = .25em,
    rounded corners = .25em,
    font = \footnotesize,
  },
  sec/.style = {
    var = black,
    draw = none,
    text = white,
    inner sep = .25em,
    font = \footnotesize,
  },
  ageplot/.style = {
    variable = \t,
    samples = 256,
    domain = 0:50,
  },
  fade/.style = { opacity = .2 },
  lab/.style = { font = \footnotesize },
  dash/.style 2 args = { dash pattern = on #1/2pt off #2/2pt },
  brace/.style = { decorate, decoration = {brace,amplitude=#1} },
  o/.tip = {Circle[length=.8mm]},
  x/.tip = {Rays[length=1mm]},
  e/.tip = {Turned Square[length=1mm]}
}
\newcommand{\shift}[3]{\begin{scope}[xshift=#1,yshift=#2]#3\end{scope}}
% -----------------------------------------------------------------------------
% symbols
\renewcommand{\d}{\textit{\dj}}
\newcommand  {\h}{\ensuremath{{\hbar}}}
\newcommand  {\p}[1][\tiny]{\raisebox{1ex}{\rotatebox{180}{#1\textit{\dj}}}}
\renewcommand{\ss}[3]{\ensuremath{{#1}^{\,#2}_{#3}}}
\newcommand{\RR} [2]{\ss{RR} {#1}{#2}}\newcommand{\RP} [2]{\ss{RP} {#1}{#2}}
\newcommand{\nRR}[2]{\ss{nRR}{#1}{#2}}\newcommand{\nRP}[2]{\ss{nRP}{#1}{#2}}
\newcommand{\tRR}[2]{\ss{tRR}{#1}{#2}}\newcommand{\tRP}[2]{\ss{tRP}{#1}{#2}}
\newcommand{\dRR}[2]{\ss{dRR}{#1}{#2}}
\def\re{\rlap{\raisebox{.4ex}{'}}}
\def\onset{\lambda}
\def\recov{\gamma}
\def\relap{\lambda\re}
